- name: dynamic yaml
  hosts: all
  become: yes
  vars:
    - ansible_uaer: ec2-user
    - ansible_password: DevOps321
  tasks:
  - name: install git
    ansible.builtin.dnf:
      name: git
      state: installed